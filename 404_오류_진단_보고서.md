# 🔍 404 오류 정확한 진단 보고서

## 📋 발견된 문제

### 주요 발견사항

1. **dashboard.html이 로드하는 컴포넌트들**:
   ```
   /static/css/dashboard_v3.css
   /static/css/dashboard_mobile_responsive.css
   /static/js/dashboard/dashboard.js
   /static/dashboard-components/sidebar.html
   /static/dashboard-components/anomalies/*.html
   /static/dashboard-components/assets/*.html
   /static/dashboard-components/reports/*.html
   /static/dashboard-components/settings/*.html
   ```

2. **문제의 원인**:
   - Node.js 서버에서 `/static/*` 경로로 요청이 들어옴
   - 하지만 `server/app.js`에서 `/dashboard` 라우트는 템플릿만 보내고 있음
   - 정적 파일 서빙 경로 문제

---

## 🔍 정확한 진단

### 서버 구조 확인

**현재 서버**: Node.js (3000번 포트)
**Flask 서버**: 실행 안됨 (gunicorn 없음)

### 라우트 확인 필요

프로덕션 서버에서 다음을 확인해야 합니다:

```bash
# 1. 어떤 라우트가 설정되어 있는지
grep -r "/dashboard" server/*.js
grep -r "/static" server/*.js

# 2. server/app.js 파일 확인
cat server/app.js | grep -A 5 "app.get.*dashboard"

# 3. 정적 파일 경로 설정 확인
cat server/app.js | grep -A 2 "express.static"
```

---

## 🚨 문제의 원인

### 1. 라우트 이중화
- Node.js 서버에서 `/dashboard` 라우트 정의
- Flask에서도 `/dashboard` 라우트 정의
- 두 서버가 충돌 가능

### 2. 정적 파일 서빙
- `/static/` 경로로 요청이 들어옴
- 어떤 서버가 이를 처리해야 하는지 불명확

### 3. 템플릿 파일 위치
- Node.js 서버는 `templates/` 폴더를 찾음
- Flask 서버는 `templates/` 폴더를 찾음
- 하지만 정적 파일은 `static/` 폴더에 있음

---

## ✅ 해결 방법

### 즉시 확인해야 할 사항

프로덕션 서버에서 실행:

```bash
# 1. server/app.js 파일 확인
cat server/app.js | grep -A 10 "app.get.*'/'"

# 2. 정적 파일 설정 확인
cat server/app.js | grep -B 5 -A 5 "express.static"

# 3. 실제 라우트 확인
curl http://localhost:3000/
curl http://localhost:3000/dashboard
curl http://localhost:3000/static/css/dashboard_v3.css
```

### 예상되는 문제

1. **server/app.js에 정적 파일 설정이 없는 경우**:
   ```javascript
   app.use('/static', express.static('static'));
   ```

2. **라우트 순서 문제**:
   - 특정 라우트가 모든 요청을 가로채는 경우

3. **템플릿 파일 경로 문제**:
   - 템플릿은 전달되지만 안의 경로가 틀림

---

## 📊 진단 체크리스트

프로덕션 서버에서 확인:

### 1. 정적 파일 접근 테스트
```bash
# CSS 파일
curl -I http://localhost:3000/static/css/dashboard_v3.css

# JS 파일
curl -I http://localhost:3000/static/js/dashboard/dashboard.js

# 컴포넌트 파일
curl -I http://localhost:3000/static/dashboard-components/sidebar.html
```

### 2. 서버 설정 확인
```bash
# server/app.js 확인
cat server/app.js | grep "express.static"
cat server/app.js | grep "app.get"
cat server/app.js | grep "/dashboard"
```

### 3. PM2 로그 확인
```bash
pm2 logs signalcraft-nodejs --lines 100
```

---

## 🎯 예상되는 해결책

### 1. server/app.js 수정 필요

```javascript
// 정적 파일 서빙 추가
app.use('/static', express.static(path.join(__dirname, '../static')));

// 대시보드 라우트 수정
app.get('/dashboard', (req, res) => {
    res.sendFile(path.join(__dirname, '../templates/customer/dashboard.html'));
});
```

### 2. Flask 서버 시작 (또는 필요없음)

만약 Flask를 사용한다면:
```bash
# Python 가상환경 활성화
source venv/bin/activate

# Flask 서버 시작
python3 app.py

# 또는 PM2로
pm2 start "python3 app.py" --name signalcraft-python
```

---

## 🔧 진단 명령어 (프로덕션 서버에서 실행)

다음 명령어들을 프로덕션 서버에서 실행하여 정확한 원인을 찾으세요:

```bash
# 1. 현재 디렉토리 확인
pwd

# 2. 파일 구조 확인
ls -la static/
ls -la templates/customer/

# 3. server/app.js 확인
cat server/app.js | head -50

# 4. 정적 파일 테스트
curl http://localhost:3000/static/css/dashboard_v3.css
curl http://localhost:3000/static/js/dashboard/dashboard.js

# 5. PM2 로그 확인
pm2 logs signalcraft-nodejs --lines 50 --nostream

# 6. 템플릿 파일 확인
cat templates/customer/dashboard.html | grep -E "href=|src=" | head -20
```

이 결과를 알려주시면 정확한 해결책을 제시할 수 있습니다!

