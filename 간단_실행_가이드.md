# 🔑 김주영님 SSH 키 추가 - 간단 실행 가이드

## 🚀 가장 간단한 방법 (PowerShell)

### Windows PowerShell에서 실행:
```powershell
.\add_juyoung_key.ps1
```

이 스크립트는 자동으로:
1. 키 파일 경로 확인 (`C:\Signal_craft\음원라벨링도구\compressor-ai-diagnosis\src\signalcraft-new.pem`)
2. WSL 경로로 변환
3. SSH 키 추가 스크립트 실행

## 🔧 WSL에서 직접 실행 (대안)

WSL 터미널을 열고:
```bash
cd ~/smartcompressor-ai-system
./quick_add_ssh_key.sh /mnt/c/Signal_craft/음원라벨링도구/compressor-ai-diagnosis/src/signalcraft-new.pem
```

## ✅ 완료 확인

스크립트가 성공적으로 실행되면:
```
✅ SSH 공개키가 성공적으로 추가되었습니다!
✨ 김주영님의 SSH 접속 권한이 활성화되었습니다!
```

## 📝 접속 테스트

김주영님이 다음 명령어로 서버에 접속할 수 있습니다:
```bash
ssh ubuntu@3.39.124.0
```

