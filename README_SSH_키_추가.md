# 🔑 김주영님 SSH 키 추가 - 빠른 시작 가이드

## ✅ 가장 간단한 방법 (WSL/Git Bash)

```bash
./quick_add_ssh_key.sh /path/to/your-ec2-key.pem
```

## 📝 상세 가이드

자세한 내용은 `SSH_키_추가_실행_가이드.md` 파일을 참고하세요.

## 🎯 제공된 SSH 공개키

김주영님(juyoung@signalcraft)의 SSH 공개키가 자동으로 스크립트에 포함되어 있습니다.

## ✨ 완료 후

스크립트 실행 후 김주영님이 다음 명령어로 서버에 접속할 수 있습니다:

```bash
ssh ubuntu@3.39.124.0
```

