# 🔐 GitHub 토큰 재생성 가이드

## ❌ 문제
현재 토큰에 `read:org` 권한이 없어서 GitHub CLI 인증이 실패합니다.

## ✅ 해결 방법: 토큰 재생성

### 1단계: 새 토큰 생성

1. **브라우저에서 열기**: https://github.com/settings/tokens

2. **"Generate new token" → "Generate new token (classic)" 클릭**

3. **토큰 설정**:
   - **Note**: `gh-cli-v2` (원하는 이름)
   - **Expiration**: 90 days 또는 No expiration
   - **필수 권한 체크** (꼭 체크하세요!):
     - ✅ **repo** (전체) - 필수
     - ✅ **read:org** - 필수 (이게 빠져있었습니다!)
     - ✅ **workflow** - GitHub Actions 사용시
   - ❗ **중요**: 세 권한 모두 체크해야 합니다!

4. **"Generate token" 클릭**

5. **토큰 복사** (`ghp_`로 시작, 한 번만 보여집니다!)

### 2단계: 인증

```bash
gh auth login
```

선택:
1. `What account?` → `GitHub.com` (Enter)
2. `Protocol?` → `HTTPS` (Enter)
3. `Authenticate Git?` → `Yes` (Enter)
4. `How to authenticate?` → `Paste an authentication token` (선택)
5. **새 토큰 붙여넣기** (Ctrl+Shift+V)

### 3단계: 확인

```bash
gh auth status
```

성공하면:
```
✓ Logged in to github.com as YOUR_USERNAME
✓ Git operations for github.com configured to use HTTPS
```

## 📋 필수 권한 체크리스트

토큰 생성 시 다음 3개 권한을 **반드시** 체크하세요:

- ✅ **repo** (전체) - 저장소 접근
- ✅ **read:org** - 조직 정보 읽기 (이게 빠졌었습니다!)
- ✅ **workflow** - GitHub Actions 사용

## 🔍 권한 확인

토큰 생성 후에도 권한을 확인할 수 있습니다:
https://github.com/settings/tokens

각 토큰을 클릭하면 부여된 권한을 확인할 수 있습니다.

---

**새 토큰 생성 후 알려주시면 PR 리뷰를 진행하겠습니다!**

