# 📖 AWS 콘솔에서 RDS 정보 확인하기 (초보자용)

## 🎯 목표
EC2 서버가 사용할 데이터베이스(DB) 정보를 찾는 것입니다.

---

## 📝 단계별 따라하기

### 1단계: AWS 콘솔 접속

1. 브라우저 열기
2. 주소창에 입력: `https://console.aws.amazon.com`
3. Enter 키 누르기
4. 대표님 AWS 계정으로 로그인

---

### 2단계: 서울 리전 선택 (중요!)

1. 화면 **맨 위 오른쪽** 보기
2. 리전 선택 드롭다운 찾기
   - 예: "미국 동부 (버지니아 북부)" 같은 글자
3. 클릭
4. 목록에서 **"아시아 태평양 (서울)"** 또는 **"Asia Pacific (Seoul)"** 선택
   - 영문으로는 `ap-northeast-2`라고 표시될 수도 있습니다

---

### 3단계: RDS 서비스 찾기

**방법 A (서비스 검색 - 추천):**
1. 화면 **맨 위 왼쪽** 보기
2. **"서비스 검색"** 박스 찾기 (검색 아이콘 모양)
3. 클릭
4. `rds` 입력
5. 나오는 목록에서 **"RDS"** 클릭

**방법 B (메뉴에서 찾기):**
1. 화면 **맨 위 왼쪽** 보기
2. **"서비스"** 클릭
3. "데이터베이스" 섹션 찾기
4. **"RDS"** 클릭

---

### 4단계: 데이터베이스 목록 보기

1. 왼쪽 메뉴에서 **"데이터베이스"** 클릭
2. 목록에 데이터베이스가 나옵니다
3. **PostgreSQL** 타입인 것을 찾기
   - 타입이 "PostgreSQL"로 표시되어 있습니다
4. 그 데이터베이스의 **이름을 클릭** (예: signalcraft, postgres 등)

---

### 5단계: 필요한 정보 복사하기

데이터베이스 상세 화면에서 다음을 찾아 복사하세요:

#### ① 엔드포인트 (Endpoint) - 가장 중요!
- 화면에서 **"연결 및 보안"** 탭 클릭
- **"엔드포인트"** 항목 찾기
- 옆에 있는 긴 주소 복사
  - 예: `signalcraft.xxxxx.ap-northeast-2.rds.amazonaws.com`
- ✅ 이게 바로 **DB_HOST** 값입니다!

#### ② 포트 (Port)
- "연결 및 보안" 탭에서
- **"포트"** 항목 확인
- 보통 **5432**입니다
- ✅ 이게 **DB_PORT** 값입니다!

#### ③ 마스터 사용자 이름
- 화면에서 **"구성"** 탭 클릭
- **"마스터 사용자 이름"** 항목 찾기
- 옆에 있는 이름 확인
  - 예: `postgres`, `admin`, `signalcraft_user` 등
- ✅ 이게 **DB_USER** 값입니다!

#### ④ 데이터베이스 이름
- "구성" 탭에서
- **"데이터베이스 이름"** 항목 찾기
- 옆에 있는 이름 확인
  - 예: `signalcraft`, `postgres`, `smartcompressor_ai` 등
- ✅ 이게 **DB_NAME** 값입니다!

---

## 📋 정리하기

확인하신 정보를 이렇게 정리하세요:

```
엔드포인트: xxx.ap-northeast-2.rds.amazonaws.com
포트: 5432
사용자명: postgres (또는 다른 이름)
데이터베이스명: signalcraft (또는 다른 이름)
```

---

## ⚠️ 만약 데이터베이스가 없다면?

1. 왼쪽 메뉴에서 "데이터베이스" 클릭했을 때 목록이 비어있으면
2. 대표님 계정에 RDS가 아직 생성되지 않은 것입니다
3. 그럼 새로 만들어야 합니다

---

## 🎯 다음 단계

위 4가지 정보를 확인하신 후:
- 메모장에 적어두시거나
- 저에게 알려주시면
- 바로 .env 파일을 수정해드리겠습니다!

---

## 💡 팁

- 각 탭(연결 및 보안, 구성 등)을 클릭하면서 정보를 찾아보세요
- 복사할 때는 주소 전체를 정확히 복사하세요
- 혹시 모르면 화면 캡처를 찍어두시면 좋습니다

---

**궁금한 점이 있으면 언제든 물어보세요!** 😊

