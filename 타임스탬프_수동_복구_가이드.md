# ESP32 타임스탬프 수동 복구 가이드

## 문제
10월 27일, 21일 데이터가 1970년으로 잘못 표시됩니다.

## 원인
이전 `fix_timestamps.js` 스크립트가 작은 숫자 타임스탬프를 잘못 변환했습니다.

## 해결 방법

프로덕션 서버에서 다음을 실행하세요:

```bash
cd /var/www/smartcompressor

# 1. 최신 코드 가져오기
git pull origin main

# 2. 복구 스크립트 실행
node scripts/recover_timestamps.js

# 3. 결과 확인
echo "복구된 파일 확인..."

# 4. 재시작
pm2 restart signalcraft-app
```

## 복구 스크립트가 작동하지 않는 경우

만약 복구 스크립트가 여전히 작동하지 않으면, 다음을 시도하세요:

```bash
# 1. 백업
cp -r data/esp32_features data/esp32_features_backup_$(date +%Y%m%d_%H%M%S)

# 2. 문제가 있는 파일 찾기
find data/esp32_features -name "*.json" -exec grep -l "1970" {} \;

# 3. 해당 파일들을 확인하고 수동으로 수정
```

## 확인 방법

복구 후 다음 URL로 확인:
https://signalcraft.kr/esp32-all-data

또는 API 직접 호출:
```bash
curl http://localhost:3000/api/esp32/all | grep -i "1970" | wc -l
```

1970이 나타나지 않으면 복구 성공입니다.

