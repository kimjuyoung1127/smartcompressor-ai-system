# 🔐 GitHub CLI 인증 완료 후 PR 리뷰

## 🚀 인증 실행

토큰이 준비되었습니다. 다음 중 하나를 선택하세요:

### 방법 1: 자동 스크립트 (빠름)

```bash
chmod +x auth_with_token.sh
./auth_with_token.sh
```

### 방법 2: 수동 인증 (권장)

```bash
gh auth login
```

그 다음:
1. `What account?` → `GitHub.com` (Enter)
2. `Protocol?` → `HTTPS` (화살표로 이동 후 Enter)
3. `Authenticate Git?` → `Yes` (Enter)
4. `How to authenticate?` → `Paste an authentication token` 선택
5. 토큰 붙여넣기: `[YOUR_TOKEN_HERE]` (개인 토큰을 직접 입력하세요)
   - WSL에서 붙여넣기: `Ctrl+Shift+V`
   - ⚠️ 토큰은 절대 공개 저장소에 커밋하지 마세요!

### 인증 확인

```bash
gh auth status
```

성공하면:
```
✓ Logged in to github.com as YOUR_USERNAME
```

## ✅ 인증 완료 후 PR 리뷰

```bash
# PR #4 자동 리뷰
./gh_pr_review.sh 4

# 또는 인터랙티브 리뷰
./gh_pr_review_interactive.sh 4

# 또는 직접 명령어
gh pr view 4
gh pr diff 4
```

---

**보안 주의**: 토큰은 절대 공개하지 마세요! 인증 후에는 `auth_with_token.sh` 파일을 삭제하시는 것을 권장합니다.

