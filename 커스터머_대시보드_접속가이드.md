# 🎛️ 커스터머 대시보드 접속 가이드

## 📍 접속 URL

### 1. 기본 대시보드 (로그인 필요)
```
http://localhost:5000/dashboard
또는
https://signalcraft.kr/dashboard
```

**요구사항**:
- 로그인 필수 (`session['user_id']` 필요)
- 미로그인 시 `/login`으로 리다이렉트

### 2. 대시보드 API 엔드포인트
```bash
# 대시보드 요약 정보
GET http://localhost:5000/api/dashboard/summary

# 매장 목록
GET http://localhost:5000/api/dashboard/stores

# 디바이스 목록
GET http://localhost:5000/api/dashboard/devices

# 분석 데이터
GET http://localhost:5000/api/dashboard/analytics

# 알림 이력
GET http://localhost:5000/api/dashboard/notifications
```

### 3. 대시보드 컴포넌트 (정적 파일)
```
GET http://localhost:5000/static/dashboard-components/sidebar.html
GET http://localhost:5000/static/dashboard-components/anomalies/anomaly-feed.html
GET http://localhost:5000/static/dashboard-components/assets/asset-feed.html
```

---

## 🚪 라우트 구조

### Flask 라우트 (app.py)

#### 1. customer_dashboard (customer_routes.py)
```python
@customer_bp.route('/dashboard')
def customer_dashboard():
    if not session.get('user_id'):
        return redirect('/login')
    return render_template('customer/dashboard.html')
```
- **URL**: `/dashboard`
- **템플릿**: `templates/customer/dashboard.html`
- **인증**: 세션 기반 인증 필요

#### 2. dashboard API (dashboard_routes.py)
```python
dashboard_bp = Blueprint('dashboard', __name__, url_prefix='/api/dashboard')
```
- **URL Prefix**: `/api/dashboard/*`
- **기능**: RESTful API 엔드포인트

#### 3. 직접 라우트 (app.py)
```python
@app.route('/dashboard')
def dashboard():
    return render_template('dashboard.html')

@app.route('/original-dashboard')
def original_dashboard():
    return render_template('customer/dashboard.html')

@app.route('/mobile_friendly_dashboard')
def mobile_friendly_dashboard():
    return render_template('customer/mobile_friendly_dashboard.html')
```
- **주의**: app.py에 여러 라우트가 혼재 (정리 필요)

### Node.js 라우트 (server/app.js)
```javascript
app.get('/dashboard', (req, res) => {
    res.sendFile(path.join(__dirname, '../templates/customer/dashboard.html'));
});
```
- **URL**: `/dashboard`
- **파일**: `templates/customer/dashboard.html`

### Nginx 설정
```nginx
location /dashboard {
    proxy_pass http://localhost:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
}
```
- **리버스 프록시**: Node.js로 전달
- **포트**: 3000

---

## 🎯 실제 접속 방법

### 방법 1: 로컬 개발 환경

#### 1. 서버 시작
```bash
# Flask 서버 시작 (포트 5000)
python app.py

# 또는 Node.js 서버 시작 (포트 3000)
node server/app.js
```

#### 2. 브라우저 접속
```bash
# Flask를 통해 접속
http://localhost:5000/dashboard

# Node.js를 통해 접속
http://localhost:3000/dashboard
```

#### 3. 로그인 필요
- 먼저 로그인 페이지 접속
- 계정 생성 또는 로그인
- 대시보드로 리다이렉트

### 방법 2: 프로덕션 환경

#### 1. 서버 확인
```bash
# 서버 상태 확인
pm2 status

# 로그 확인
pm2 logs signalcraft-nodejs
pm2 logs signalcraft-python
```

#### 2. 브라우저 접속
```
https://signalcraft.kr/dashboard
```

#### 3. 인증
- 카카오 로그인
- 또는 이메일/비밀번호 로그인

---

## 📋 대시보드 기능

### 1. 기본 기능
- **실시간 모니터링**: IoT 센서 데이터 시각화
- **이상 징후 감지**: AI 기반 이상 패턴 탐지
- **자산 관리**: 매장 및 디바이스 관리
- **알림 관리**: 알림 이력 및 설정
- **리포트**: 성능 분석 및 트렌드 리포트

### 2. 컴포넌트 구조
```
dashboard.html (베이스)
├─ sidebar.html (네비게이션)
├─ /anomalies/ (이상징후)
│   ├─ anomaly-feed.html
│   ├─ anomaly-trend.html
│   ├─ today-briefing.html
│   └─ threat-details-card.html
├─ /assets/ (자산관리)
│   ├─ asset-feed.html
│   ├─ asset-details-card.html
│   └─ asset-trend.html
├─ /reports/ (리포트)
│   └─ reports-main.html
└─ /settings/ (설정)
    └─ settings-main.html
```

### 3. API 엔드포인트

#### 요약 정보
```bash
GET /api/dashboard/summary?store_id=xxx
```
**응답**:
```json
{
  "success": true,
  "summary": {
    "total_stores": 10,
    "online_devices": 25,
    "offline_devices": 2,
    "alerts_today": 3
  },
  "energy_data": {...},
  "device_status": {...}
}
```

#### 매장 목록
```bash
GET /api/dashboard/stores?owner_id=xxx
```

#### 디바이스 목록
```bash
GET /api/dashboard/devices?store_id=xxx
```

#### 분석 데이터
```bash
GET /api/dashboard/analytics?store_id=xxx&days=30
```

---

## ⚠️ 문제 해결

### 1. 로그인 실패
**증상**: 대시보드 접속 시 `/login`으로 리다이렉트

**원인**:
- 세션이 만료됨
- 로그인이 안 됨

**해결**:
```bash
# 세션 확인
# 브라우저 개발자 도구 → Application → Cookies
# session_id 확인

# 로그인 다시 시도
```

### 2. 404 에러
**증상**: `/dashboard` 접속 시 404 에러

**원인**:
- 라우트가 등록되지 않음
- 서버가 실행되지 않음

**해결**:
```bash
# Flask 서버 확인
python app.py

# Node.js 서버 확인
node server/app.js

# PM2 확인 (프로덕션)
pm2 status
pm2 restart signalcraft-nodejs
pm2 restart signalcraft-python
```

### 3. 컴포넌트 로드 실패
**증상**: 대시보드는 보이지만 일부 섹션이 비어있음

**원인**:
- 컴포넌트 파일이 없음
- 라우트 문제

**해결**:
```bash
# 컴포넌트 파일 확인
ls static/dashboard-components/

# 파일 생성/복구
git checkout HEAD -- static/dashboard-components/

# 정적 파일 서빙 확인
curl http://localhost:5000/static/dashboard-components/sidebar.html
```

### 4. CORS 오류
**증상**: API 호출 시 CORS 에러

**원인**:
- CORS 설정 부족

**해결**:
```python
# app.py에서 CORS 설정 확인
from flask_cors import CORS
CORS(app, resources={r"/api/*": {"origins": "*"}})
```

---

## 🔧 개발자 도구 활용

### 1. 브라우저 콘솔
```javascript
// 네트워크 요청 확인
fetch('/api/dashboard/summary')
  .then(r => r.json())
  .then(data => console.log(data));

// 세션 확인
fetch('/api/auth/verify')
  .then(r => r.json())
  .then(data => console.log(data));
```

### 2. API 테스트
```bash
# curl로 API 테스트
curl http://localhost:5000/api/dashboard/summary

# 로그인 후 세션 사용
curl -b cookies.txt http://localhost:5000/dashboard
```

### 3. 디버깅
```python
# Flask 로그 확인
import logging
logging.basicConfig(level=logging.DEBUG)

# 라우트 확인
print(app.url_map)
```

---

## 📊 성능 체크

### 1. 로드 시간 측정
```javascript
performance.mark('dashboard-start');
// 대시보드 로드
performance.mark('dashboard-end');
performance.measure('dashboard-load', 'dashboard-start', 'dashboard-end');
```

### 2. API 응답 시간
```bash
# curl 시간 측정
time curl http://localhost:5000/api/dashboard/summary
```

### 3. 메모리 사용량
```python
# Flask
import psutil
print(psutil.virtual_memory())

# PM2
pm2 monit
```

---

## 🎯 권장 사항

### 1. 라우트 정리 필요
현재 여러 곳에 `/dashboard` 라우트가 정의되어 있습니다:
- `routes/customer_routes.py` (customer_bp)
- `app.py` (직접 라우트)
- `server/app.js` (Node.js)

**권장**: 하나의 통합된 라우트로 통합

### 2. 인증 강화
현재 세션 기반 인증만 사용 중입니다.

**권장**:
- JWT 토큰 추가
- 역할 기반 접근 제어 (RBAC)
- CSRF 보호

### 3. 캐싱 전략
대시보드 데이터는 자주 변경되지 않습니다.

**권장**:
- Redis 캐싱
- CDN 활용 (정적 파일)
- 압축 적용

---

## 📝 체크리스트

### 접속 전 확인
- [ ] 서버 실행 확인
- [ ] 로그인 상태 확인
- [ ] 네트워크 연결 확인
- [ ] CORS 설정 확인

### 접속 후 확인
- [ ] 대시보드 UI 로드 확인
- [ ] 사이드바 메뉴 동작 확인
- [ ] 데이터 표시 확인
- [ ] 에러 없음 확인

---

## 🔗 관련 파일

### 템플릿
- `templates/customer/dashboard.html` - 메인 대시보드
- `templates/customer/refactored-dashboard.html` - 리팩토링된 대시보드
- `templates/customer/mobile_friendly_dashboard.html` - 모바일 대시보드

### 라우트
- `routes/customer_routes.py` - customer_bp
- `routes/dashboard_routes.py` - dashboard_bp (API)
- `app.py` - 직접 라우트

### 서비스
- `services/dashboard_service.py` - 대시보드 데이터 서비스
- `services/analytics_service.py` - 분석 서비스
- `services/notification_management_service.py` - 알림 서비스

### 정적 파일
- `static/dashboard-components/` - 대시보드 컴포넌트
- `static/css/dashboard_v3.css` - 스타일시트
- `static/css/dashboard_mobile_responsive.css` - 모바일 스타일

---

**업데이트**: 2025-10-23  
**버전**: 1.0  
**상태**: ✅ 사용 가능

