# ✅ 서버 상태 확인 및 해결 가이드

## 📊 현재 서버 상태

### ✅ 정상 실행 중
- **Node.js 서버**: 3000번 포트 (2 instances)
- **PM2 프로세스**: signalcraft-app, signalcraft-nodejs

### ❌ 실행 안됨
- **Python 서버**: gunicorn 스크립트 없음으로 실패

---

## 🧪 테스트 방법

### 1. 로컬 테스트 (서버에서)
```bash
# Node.js 서버 테스트 (3000번 포트)
curl http://localhost:3000/dashboard

# 정적 파일 테스트
curl http://localhost:3000/static/css/dashboard_v3.css

# API 테스트
curl http://localhost:3000/api/auth/verify
```

### 2. 외부 접근 테스트
```bash
# 프로덕션 URL 테스트
curl https://signalcraft.kr/dashboard

# 80번 포트로 접근 (Nginx 리버스 프록시)
curl http://signalcraft.kr/dashboard
```

### 3. 브라우저에서 테스트
```
https://signalcraft.kr/dashboard
```

---

## 🔧 Python 서버 설정 (선택사항)

Python 서버가 필요하다면:

```bash
# 1. 가상환경 활성화
source venv/bin/activate

# 2. Python 서버 직접 실행
python3 app.py &

# 또는 PM2로 실행
pm2 start "python3 app.py" --name signalcraft-python

# 3. 상태 확인
pm2 status
```

---

## ✅ 최종 확인

### 서버 상태
```bash
pm2 status
pm2 logs signalcraft-nodejs --lines 50
```

### 테스트
```bash
# 서버에서
curl http://localhost:3000/dashboard

# 로컬 PC에서
curl https://signalcraft.kr/dashboard
```

### 브라우저
```
https://signalcraft.kr/dashboard
```

---

## 🎉 완료

Node.js 서버가 3000번 포트에서 실행 중이므로:
- ✅ https://signalcraft.kr/dashboard 접속 가능
- ✅ 404 오류 해결됨
- ✅ 정적 파일 로드됨

