# 🚀 서버 배포 필요 안내

## 📋 현재 상황

### 문제
- ✅ 코드 수정 완료
- ✅ GitHub에 푸시 완료
- ❌ **프로덕션 서버(signalcraft.kr)에 배포되지 않음**

### 증상
```
https://signalcraft.kr/dashboard → 404 오류
```

---

## 🔍 확인된 사항

### 1. 코드 상태
- ✅ GitHub에 최신 커밋 푸시됨
- ✅ 커밋: `c2914e5` (Flask 정적 파일 경로 설정 수정)

### 2. GitHub Actions
- ✅ 자동 배포 워크플로우 존재
- ⏳ 실행 대기 중 (push 트리거 시 자동 실행)

### 3. 서버 상태
- ❓ PM2 프로세스 없음 (현재 로컬 환경)
- ❓ 프로덕션 서버 상태 확인 필요

---

## 🛠️ 해결 방법

### 방법 1: GitHub Actions 자동 배포 (권장)

GitHub Actions가 자동으로 배포해야 합니다. 상태를 확인하세요:

**확인 방법**:
1. GitHub 저장소 접속
2. `Actions` 탭 클릭
3. 최근 워크플로우 실행 상태 확인
4. 실행이 있으면 "Deploy to SIGNALCRAFT EC2" 작업 확인

**워크플로우 파일**: `.github/workflows/deploy.yml`

### 방법 2: 수동 배포 스크립트 실행

만약 자동 배포가 작동하지 않으면:

```bash
# 프로덕션 서버에 SSH 접속
ssh ubuntu@signalcraft.kr

# 서버에서 실행
cd /home/ubuntu/smartcompressor-ai-system

# 최신 코드 가져오기
git pull origin main

# Python 의존성 설치 (필요시)
pip install -r requirements.txt

# PM2 재시작
pm2 restart signalcraft-python
pm2 restart signalcraft-nodejs

# 서비스 상태 확인
pm2 status
pm2 logs
```

### 방법 3: GitHub 워크플로우 수동 실행

GitHub 저장소에서:

1. `Actions` 탭 클릭
2. `Deploy to SIGNALCRAFT EC2` 워크플로우 선택
3. **"Run workflow"** 버튼 클릭
4. main 브랜치 선택 → **"Run workflow"** 클릭

---

## 📊 배포 스크립트 내용

### `.github/workflows/deploy.yml` (자동 배포)

```yaml
name: Deploy to SIGNALCRAFT EC2

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Deploy to EC2
      uses: appleboy/ssh-action@v1.0.0
      with:
        host: ${{ secrets.EC2_HOST }}
        username: ${{ secrets.EC2_USER }}
        key: ${{ secrets.EC2_SSH_KEY }}
        script: |
          cd /home/ubuntu/smartcompressor-ai-system
          
          # Git pull 최신 코드
          echo "📥 최신 코드 가져오는 중..."
          git pull origin main
          
          # 서비스 재시작
          echo "🔄 서비스 재시작 중..."
          pm2 restart all || pm2 start all
          
          echo "🎉 배포 완료!"
```

---

## ⏰ 예상 시간

- **GitHub Actions**: 2-5분
- **수동 배포**: 3-5분
- **서버 재시작**: 1-2분

---

## ✅ 확인 방법

### 배포 완료 후 확인

```bash
# 1. 브라우저에서 확인
https://signalcraft.kr/dashboard

# 2. curl로 API 테스트
curl https://signalcraft.kr/api/auth/verify

# 3. 정적 파일 테스트
curl https://signalcraft.kr/static/css/dashboard_v3.css
```

### 성공 시
- ✅ 대시보드 페이지 로드됨
- ✅ 404 오류 사라짐
- ✅ CSS/JS 파일 정상 로드

---

## 🚨 문제가 계속되면

### 1. GitHub Actions 상태 확인
```
저장소 → Actions 탭 → 최근 워크플로우 확인
```

### 2. 서버 로그 확인
```bash
ssh ubuntu@signalcraft.kr
pm2 logs signalcraft-python --lines 50
pm2 logs signalcraft-nodejs --lines 50
```

### 3. 수동 배포
위의 "방법 2: 수동 배포 스크립트 실행" 참조

---

## 📋 체크리스트

### GitHub Actions 확인
- [ ] Actions 탭에서 최근 워크플로우 확인
- [ ] 배포 작업 실행 상태 확인
- [ ] 성공/실패 로그 확인

### 수동 배포 시
- [ ] SSH 접속 성공
- [ ] git pull 성공
- [ ] 서비스 재시작 성공
- [ ] 테스트 통과

### 배포 확인
- [ ] https://signalcraft.kr/dashboard 접속 테스트
- [ ] 404 오류 해결 확인
- [ ] 페이지 정상 로드 확인

---

**다음 단계**: GitHub Actions 상태를 확인하고 필요시 수동 배포를 진행하세요!

