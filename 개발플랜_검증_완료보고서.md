🚀 SignalCraft DB 마이그레이션 완료 및 개발 2단계 착수 보고서

상태

🟢 완료 (Completed)

문서 목적

1. '개발 계획' 수행을 위해 DB 스키마의 치명적 오류를 수정한 1차 마이그레이션 작업을 보고.



 2. 이로써 실행 가능해진 **'인증 시스템 개편(Phase 1)'**의 다음 개발 작업을 지시.

작업 일자

2025-11-04

작성자

김주영 (개발 담당)

1. 개요 (Executive Summary)

기존 smartcompressor_ai 데이터베이스 스키마는 우리가 수행하려던 **'개발 계획(Dev Plan)'**과 근본적인 충돌이 있었습니다.

이에, 대표님의 기존 테이블 구조(이름)는 최대한 유지하면서, '개발 계획' 수행에 방해가 되는 **3가지 치명적인 문제점(시간대, 사용자 통합, 세션)**만을 해결하는 **'최소 침습(Minimal Patch) 스키마 개선'**을 성공적으로 완료했습니다.

이제 데이터베이스의 뼈대가 바로 잡혔으므로, 즉시 **'개발 계획 1단계: 인증 시스템 개편'**을 위한 실제 코드 수정 작업에 착수해야 합니다.

2. 우리가 해결한 문제 (The "Why")

우리는 '개발 계획'을 시작조차 할 수 없었던 3가지 치명적인 문제를 해결했습니다.

[해결됨] 시간대(Timezone) 오류: 모든 timestamp 컬럼을 TIMESTAMPTZ로 변경하여, KST/UTC 시간 꼬임 문제를 원천 차단했습니다.

[해결됨] '역할 기반 인증(RBAC)' 모순:

experts(라벨러) 테이블을 users 테이블로 통합했습니다.

users 테이블에 roles 배열 컬럼을 추가했습니다.

labels 테이블이 users를 참조하도록 변경했습니다.

결과: 이제 "관리자이면서 라벨러"인 사용자를 만들 수 있으며, 로그인한 사용자가 라벨링 작업을 할 수 있습니다.

[해결됨] '세션' 병목: 비표준이었던 sessions 테이블을 connect-pg-simple 라이브러리(Node.js)와 100% 호환되는 표준 테이블로 교체했습니다.

3. 작업 완료 검증 (The "Proof")

EC2 서버(smartcompressor_ai DB)에서 psql 명령어로 확인한 결과, 스키마가 성공적으로 변경되었습니다.

A. 테이블 목록 (\dt)

experts 테이블이 제거되고, users로 통합되었습니다.

sessions 테이블이 표준 규격으로 교체되었습니다.

Schema

Name

Type

Owner

public

ai_analysis_results

table

postgres

public

anomalies

table

postgres

public

audio_files

table

postgres

public

devices

table

postgres

public

labeling_stats

table

postgres

public

labels

table

postgres

public

monitoring_data

table

postgres

public

sensor_readings

table

postgres

public

sensor_statistics

table

postgres

public

sessions

table

postgres

public

stores

table

postgres

public

user_store_access

table

postgres

public

users

table

postgres

B. users 테이블 (\d users)

created_at, last_login 등이 timestamp with time zone (TIMESTAMPTZ)으로 변경되었습니다.

roles 컬럼이 character varying(50)[] (배열) 타입으로 추가되었습니다.

C. labels 테이블 (\d labels)

labeler_id (varchar) 컬럼이 제거되었습니다.

labeler_user_id (integer) 컬럼이 추가되었고, users(id)를 정상적으로 참조(Foreign Key)합니다.

D. sessions 테이블 (\d sessions)

sid(PK), sess(json), expire(TIMESTAMT_Z) 3개의 컬럼을 가진 표준 테이블로 교체되었습니다.

4. 🚀 다음 단계: "개발 계획" 코드 수정 착수

데이터베이스가 준비되었으니, 이제 **로컬 PC (C:\Users\gmdqn\signalcraft)**로 돌아와 **'개발 계획(Dev Plan)'**을 수행하기 위한 애플리케이션 코드 수정을 시작해야 합니다.

이 작업은 feature/auth-system-upgrade 같은 새 브랜치를 생성하여 진행하는 것을 강력히 권장합니다.

Phase 1: 인증 시스템 개편 (Node.js)

1. (필수) authRoutes.js 수정

파일: server/routes/authRoutes.js

작업: 기존 SQLiteDatabaseService를 require하는 코드를 삭제합니다.

변경: services/database_service.js (PostgreSQL Pool)을 require하고, login/register 로직이 PostgreSQL users 테이블을 바라보도록 쿼리를 수정합니다.

bcrypt.compare() 로직은 이미 있으므로 재활용합니다.

2. (필수) 세션 저장소 변경

파일: server.js (또는 app.js 등 Express 앱 메인 파일)

작업: SQLite 기반 세션 코드를 삭제합니다.

변경: connect-pg-simple 라이브러리를 사용하도록 코드를 수정하고, database_service.js의 PostgreSQL Pool을 연결합니다. (개발 계획서 B안 참고)

npm install express-session connect-pg-simple 명령어가 필요합니다.

3. (필수) Python(Flask) 인증 코드 제거

파일: app.py

작업: enhanced_auth_bp (Flask 인증)을 import하고 register_blueprint하는 라인을 주석 처리 또는 삭제합니다.

파일: routes/enhanced_auth_routes.py

작업: 이 파일은 더 이상 필요 없으므로 삭제합니다.

4. (필수) SQLite 의존성 완전 제거

파일: package.json

작업: "sqlite3": "..." 라인을 찾아 삭제합니다.

파일: services/sqlite_database_service.js

작업: 이 파일은 더 이상 필요 없으므로 삭제합니다.



Phase 2: AI 라벨링 툴 연동 (Node.js)

1. (필수) saveLabel API 수정

파일: 라벨 저장을 담당하는 API 라우트 (예: server/routes/labelingRoutes.js 또는 database_service.js의 saveLabel 메서드)

작업: 하드코딩된 'default_expert' 값을 수정합니다.

변경: 라벨링 API는 Phase 1의 인증 시스템을 통과해야 합니다. saveLabel 함수가 req.session.user.id (실제 로그인한 사용자의 ID)를 인자로 받아, labels 테이블의 labeler_user_id 컬럼에 저장하도록 로직을 변경합니다.

5. 최종 결론

데이터베이스라는 '인프라' 작업은 성공적으로 완료되었습니다. 이제부터는 '애플리케이션 코드'를 수정하는 '개발' 작업입니다.

위의 '다음 단계' 체크리스트에 따라 코드 수정을 완료한 후, git push를 통해 EC2 서버에 배포하면, SignalCraft는 마침내 SQLite의 의존성에서 벗어나 PostgreSQL 기반의 안정적인 통합 인증 시스템을 갖추게 됩니다.