# 🔍 EC2 리전 및 RDS 확인 방법

## 현재 상황

- **EC2 서버**: 3.39.124.0
- **현재 .env의 DB**: `signalcraft.cb8e6ea8w70p.ap-northeast-2.rds.amazonaws.com` (서울, 김주영님 계정)

## 가장 빠른 확인 방법

### AWS 콘솔에서 직접 확인 (1분 소요)

1. **AWS 콘솔 접속**
   - https://console.aws.amazon.com
   - 대표님 계정으로 로그인

2. **EC2 서비스로 이동**
   - 왼쪽 상단 서비스 검색에서 "EC2" 입력
   - EC2 대시보드로 이동

3. **인스턴스 찾기**
   - 왼쪽 메뉴에서 "인스턴스" 클릭
   - 목록에서 IP `3.39.124.0`를 가진 인스턴스 찾기
   - 또는 공인 IP로 검색

4. **리전 확인**
   - 인스턴스를 선택하면 상세 정보에 **"리전"** 표시됨
   - 예: `미국 동부 (버지니아) us-east-1` 또는 `아시아 태평양 (서울) ap-northeast-2`

5. **해당 리전으로 이동**
   - 우측 상단에서 위에서 확인한 리전 선택

6. **RDS 확인**
   - 서비스 검색에서 "RDS" 입력
   - 왼쪽 메뉴에서 "데이터베이스" 클릭
   - PostgreSQL 데이터베이스 선택

7. **필요한 정보 확인**
   - **엔드포인트** (예: `xxx.us-east-1.rds.amazonaws.com`)
   - **포트** (보통 5432)
   - **마스터 사용자 이름**
   - **데이터베이스 이름**

## 확인하신 정보 알려주시면

다음 정보를 알려주시면 올바른 .env 파일 내용을 작성해드리겠습니다:

```
리전: ?
엔드포인트: ?
사용자명: ?
데이터베이스명: ?
비밀번호: ? (비밀번호는 본인이 알고 계신 것으로 설정)
```

## 예상 시나리오

### 시나리오 1: EC2가 us-east-1에 있는 경우

```env
DB_HOST=대표님_RDS_엔드포인트.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=signalcraft
DB_USER=postgres
DB_PASSWORD=설정하신_비밀번호

DB_POOL_SIZE=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
```

### 시나리오 2: EC2가 ap-northeast-2에 있는 경우

그런데 현재 .env의 DB도 ap-northeast-2이므로:
- 같은 리전이지만 다른 계정의 RDS
- 대표님 계정의 RDS 엔드포인트로 변경 필요

```env
DB_HOST=대표님_RDS_엔드포인트.ap-northeast-2.rds.amazonaws.com
DB_PORT=5432
DB_NAME=signalcraft
DB_USER=대표님_사용자명
DB_PASSWORD=대표님_비밀번호

DB_POOL_SIZE=20
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
```

## 주의사항

⚠️ **보안 그룹**: RDS의 보안 그룹이 EC2의 보안 그룹을 허용해야 합니다.
⚠️ **VPC**: EC2와 RDS가 같은 VPC에 있거나 VPC 피어링이 설정되어야 합니다.

