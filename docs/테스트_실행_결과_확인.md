# 테스트 실행 결과 확인 가이드

## 📊 테스트 결과 해석

### 성공적인 테스트 실행

```
======================== test session starts =========================
platform linux -- Python 3.12.3, pytest-8.4.2
collected 16 items

tests/test_anomaly_detection_modules/test_decibel_filter.py::TestDecibelFilter::test_init PASSED
tests/test_anomaly_detection_modules/test_decibel_filter.py::TestDecibelFilter::test_filter_no_input PASSED
...
======================== 16 passed in 2.34s =========================
```

**의미**: 모든 테스트가 통과했습니다! ✅

---

### 테스트 실패 시

```
tests/test_anomaly_detection_modules/test_decibel_filter.py::TestDecibelFilter::test_init FAILED
...
FAILED tests/test_anomaly_detection_modules/test_decibel_filter.py::TestDecibelFilter::test_init
```

**의미**: 일부 테스트가 실패했습니다. 오류 메시지를 확인하세요.

---

## 🔍 다음 단계

### 1. 테스트 결과 확인

테스트가 완료되면 다음을 확인하세요:
- ✅ **PASSED**: 테스트 통과
- ❌ **FAILED**: 테스트 실패 (오류 메시지 확인)
- ⚠️ **ERROR**: 테스트 실행 오류

### 2. 커버리지 확인 (선택사항)

```bash
# 커버리지 포함 실행
pytest tests/ --cov=services/anomaly_detection_modules --cov-report=html

# HTML 리포트 확인
# htmlcov/index.html 파일을 브라우저에서 열기
```

### 3. 특정 테스트만 실행

```bash
# 특정 모듈만 테스트
pytest tests/test_anomaly_detection_modules/test_decibel_filter.py -v

# 특정 테스트만 실행
pytest tests/test_anomaly_detection_modules/test_decibel_filter.py::TestDecibelFilter::test_init -v
```

---

## 💡 팁

1. **테스트가 느리면**: `-v` 옵션을 제거하면 더 빠르게 실행됩니다.

2. **커버리지 확인**: `--cov-report=html` 옵션으로 HTML 리포트를 생성할 수 있습니다.

3. **실패한 테스트만 다시 실행**: `pytest --lf` (last failed)

---

**테스트가 완료되면 결과를 공유해 주세요!**

