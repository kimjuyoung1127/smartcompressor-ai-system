# PR #4 충돌 해결 실행 가이드

## 실행 방법

**WSL 터미널에서 실행하세요!**

```bash
cd ~/smartcompressor-ai-system
chmod +x resolve_conflict_simple.sh
./resolve_conflict_simple.sh
```

---

## 스크립트가 하는 일

1. ✅ 원격 브랜치 정보 가져오기
2. ✅ PR 브랜치로 체크아웃
3. ✅ main 브랜치 merge
4. ✅ 충돌 발생 시 자동 해결:
   - `nginx_signalcraft_config.conf` 파일의 충돌 해결
   - PR 버전 유지 (system/ 디렉토리로 이동한 것)
5. ✅ 커밋 및 원격에 푸시

---

## 주의사항

- **WSL 터미널에서 실행해야 합니다!**
- GitHub CLI가 인증되어 있어야 합니다 (`gh auth status` 확인)
- 충돌이 자동으로 해결되지 않으면 수동 해결이 필요합니다

---

## 실행 후 확인

PR #4 페이지에서:
- ✅ "This branch has conflicts" 메시지가 사라졌는지 확인
- ✅ "Merge pull request" 버튼이 활성화되었는지 확인

---

**지금 바로 WSL 터미널에서 실행하세요!**

