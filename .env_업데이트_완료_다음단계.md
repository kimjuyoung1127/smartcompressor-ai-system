# ✅ .env 파일 업데이트 완료!

## 완료된 작업

✅ .env 파일을 localhost로 업데이트 완료
- DB_HOST: localhost
- DB_PORT: 5432
- DB_NAME: signalcraft
- DB_USER: postgres
- DB_PASSWORD: signalcraft6898

---

## 다음 단계: PostgreSQL 설정 및 연결 테스트

### 1단계: PostgreSQL 설정 및 테스트 실행

WSL 터미널에서:
```bash
chmod +x setup_postgres_and_test.sh
./setup_postgres_and_test.sh
```

이 스크립트가 수행하는 작업:
1. ✅ postgres 사용자 비밀번호 설정
2. ✅ signalcraft 데이터베이스 확인/생성
3. ✅ PostgreSQL 인증 설정 확인
4. ✅ psql로 연결 테스트
5. ✅ Node.js로 연결 테스트

### 2단계: 서버 재시작 (필요한 경우)

애플리케이션이 실행 중이면 재시작:
```bash
ssh ubuntu@3.39.124.0
cd /home/ubuntu/smartcompressor-ai-system

# PM2 사용 중이면
pm2 restart all

# 또는 직접 실행 중이면
# 서버를 중지하고 다시 시작
```

### 3단계: 연결 확인

애플리케이션 로그 확인:
```bash
pm2 logs
# 또는
tail -f 서버로그파일
```

---

## 문제 해결

### 연결이 실패하는 경우

#### 방법 1: pg_hba.conf 수정
EC2 서버에서:
```bash
sudo nano /etc/postgresql/16/main/pg_hba.conf
```

다음 줄을 찾아서 수정:
```
# 원래:
local   all             all                                     peer
host    all             all             127.0.0.1/32            scram-sha-256

# 또는 md5로 변경:
local   all             all                                     md5
host    all             all             127.0.0.1/32            md5
```

PostgreSQL 재시작:
```bash
sudo systemctl restart postgresql
```

#### 방법 2: 비밀번호 없이 접속 (ubuntu 사용자)
만약 비밀번호 방식이 작동하지 않으면:
```bash
# ubuntu 사용자로 접속
psql -U ubuntu -d signalcraft
```

가능하면 .env를 다음과 같이 변경:
```env
DB_USER=ubuntu
DB_PASSWORD=
```

---

## 확인 사항

✅ .env 파일: localhost로 설정됨  
⏳ PostgreSQL 비밀번호: 설정 필요  
⏳ signalcraft 데이터베이스: 확인/생성 필요  
⏳ 연결 테스트: 실행 필요  

---

**위 스크립트를 실행하시면 자동으로 설정과 테스트가 진행됩니다!** 😊

